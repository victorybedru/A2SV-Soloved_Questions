class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        count= Counter(nums)
        n= len(nums)
        res=[]
        for num in nums:
            if count[num]> n/3 and num not in res:
                res.append(num)
        return res
