class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        freq= {}
        for i in nums:
            freq[i]= freq.get(i,0)+1
        for value, key in freq.items():
            if key==1:
                return value
