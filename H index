class Solution:
    def hIndex(self, citations: List[int]) -> int:
        citations.sort(reverse=True)
        h=0
        for ind, val in enumerate(citations):
            if val>= ind+1:
                h=ind+1
            else:
                break
        return h
