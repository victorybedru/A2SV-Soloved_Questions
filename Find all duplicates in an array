class Solution(object):
    def findDuplicates(self, nums):
        freq={}
        res=[]
        for i in nums:
            freq[i]= freq.get(i,0)+1
        for key,value in freq.items():
            if value==2:
                res.append(key)
        return res
